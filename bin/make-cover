#! /usr/bin/env perl
# ABSTRACT: mu
# PODNAME: make-cover

# $Id$
# $URL$

use FindBin;
use lib "$FindBin::RealBin/../lib";
use Audio::FlacJacket;

my $file = shift;

$file =~ s/.(jpg|png)$//g;

Audio::FlacJacket::MakeCover( $file );
