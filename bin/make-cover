#! /opt/perl/bin/perl
# PODNAME: make-cover
# $Id$
# $URL$

use FindBin;
use lib "$FindBin::RealBin/../lib";
use FlacJacket;

my $file = shift;

$file =~ s/.(jpg|png)$//g;

FlacJacket::MakeCover( $file );
