#! /opt/perl/bin/perl -w
# PODNAME: ogg2mp3
# $Id$
# $URL$

use strict;
$|++;

foreach( <*/*.ogg> ) {
  my( $path ) = /(.*)\.ogg/;
  my $newpath = "$path.mp3";
  print "processing $_...";
  `ogg123 -d wav $_ -f - | lame - $newpath`;
  print "done.\n"
}
